[package]
name = "smpl_evidence"
version = "0.1.0"
edition = "2021"
description = "CDC evidence normalization for smplcache"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
