[package]
name = "smpl_repair"
version = "0.1.0"
edition = "2021"
description = "SQL repair plan generation for smplcache"
license = "Apache-2.0"

[dependencies]
smpl_evidence = { path = "../smpl_evidence" }
smpl_cert = { path = "../smpl_cert" }
serde = { version = "1", features = ["derive"] }
